import { LightLevelPipe } from './light-level.pipe';

describe('LightLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new LightLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
